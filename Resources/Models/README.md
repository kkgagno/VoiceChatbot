# Local Face Models

Place local-only face model files here.

Expected files:

- `haarcascade_frontalface_default.xml` for first-stage CPU face presence detection.
- `face-embedding.onnx` later, when local identity embeddings are implemented.

Do not store private face photos in this folder. Do not log or upload face embeddings.
